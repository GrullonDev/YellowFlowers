name: yellow_flowers
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  cupertino_icons: ^1.0.8
  firebase_core: ^3.5.0
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  http: ^1.3.0
  intl: ^0.20.2
  just_audio: ^0.9.46
  provider: ^6.1.2

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
